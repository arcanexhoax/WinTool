## v0.9.4
- Fixed `Copy path to selected/opened item` and `Copy name of selected/opened item` commands
- Fixed autoselection of file name in `Create file` and arguments in `Run file with arguments` windows
- Fixed `Input popup` disabling
- Fixed `Always run as administrator` option

## v0.9.3
- Fixed empty keyboard layouts list in the `Input popup`
- Added `Run as admin` option for command `Run file with arguments`
- Added `Open current folder in command prompt as admin` shortcut
- Added `Run selected file as admin` shortcut
- Made `Run file with arguments` command work with any files

## v0.9.2
- Fixed shortcuts working when WinTool is running in the background
- Fixed `Input popup` in WPF/WinUI/etc apps
- Added tracking of keyboard layouts order
- Added `Always run as administrator` setting

## v0.9.1
- Fixed searching for active explorer tab in Windows 11
- Added desktop support for explorer shortcuts

## v0.9.0
- Updated the application style to match the Fluent theme
- Fixed crashing of the application with the `Create File` and `Run file with arguments` commands

## v0.8.0
- Added possibility to edit shortcuts

## v0.7.1
- Fixed explorer commands on Windows 11
- Fixed display of current keyboard layout in console apps (popup still has wrong position on Windows 11)
- Hide the `Input popup` after user starts typing

## v0.7.0
- Added `Input popup` near active caret in any app

## v0.6.3
- Fixed recognition of active explorer tab (Windows 11)
- Fixed focusing on modal windows

## v0.6.2
- Fixed Windows 11 explorer window search
- Made single instance application

## v0.6.1
- Added `Change file properties` shortcut
- Added caching for inputs to the `Create file` and `Run file with arguments` windows
- Fixed file creation with UAC in the background mode

## v0.6.0
First release