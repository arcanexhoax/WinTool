<UserControl x:Class="WinTool.View.ShortcutsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:r="clr-namespace:WinTool.Properties"
             xmlns:views="clr-namespace:WinTool.View"
             xmlns:viewmodel="clr-namespace:WinTool.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:ShortcutsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="10">
        <StackPanel>
            <views:ShortcutView DataContext="{Binding ChangeFilePropertiesViewModel}" />
            <views:ShortcutView DataContext="{Binding RunWithArgsViewModel}" />
            <views:ShortcutView DataContext="{Binding CreateFileViewModel}" />
            <views:ShortcutView DataContext="{Binding FastFileCreationViewModel}" />
            <views:ShortcutView DataContext="{Binding SelectedItemCopyPathViewModel}" />
            <views:ShortcutView DataContext="{Binding SelectedItemCopyNameViewModel}" />
            <views:ShortcutView DataContext="{Binding OpenFolderInCmdViewModel}" />
        </StackPanel>
    </Border>
</UserControl>
