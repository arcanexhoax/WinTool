<?xml version="1.0" encoding="utf-8"?>
<local:WindowBase x:Class="WinTool.Views.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:local="using:WinTool.Views"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:p="using:WinTool.Properties"
                  mc:Ignorable="d"
                  d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    <local:WindowBase.SystemBackdrop>
        <MicaBackdrop />
    </local:WindowBase.SystemBackdrop>


    <Grid RowDefinitions="auto, *">
        <TitleBar Title="{x:Bind p:Resources.WinTool}"
                  x:Name="titleBar">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Resources/icon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        IsPaneToggleButtonVisible="False"
                        PaneDisplayMode="Left"
                        OpenPaneLength="260"
                        Margin="0 10 0 0"
                        Loaded="OnNavViewLoaded"
                        SelectionChanged="OnSelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="{x:Bind p:Resources.Shortcuts}"
                                    Icon="Keyboard"
                                    x:Name="ShortcutsItem"/>
                <NavigationViewItem Content="{x:Bind p:Resources.Features}"
                                    x:Name="FeaturesItem">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEA80;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="Root"/>
        </NavigationView>
    </Grid>
</local:WindowBase>
