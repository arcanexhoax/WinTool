<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:WinTool.UI.Controls">
    <Style TargetType="{x:Type controls:Shortcut}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Shortcut}">
                    <ItemsControl ItemsSource="{TemplateBinding Tokens}"
                                  AlternationCount="8">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Margin" Value="6 0 0 0" />

                                <Style.Triggers>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                        <Setter Property="Margin" Value="0" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.ItemContainerStyle>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="TokenBorder"
                                        Padding="12 6"
                                        Background="{DynamicResource AccentFillColorDefaultBrush}"
                                        CornerRadius="4"
                                        MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource Self}}">
                                    <Grid MinHeight="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=FontSize}"
                                          MinWidth="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=FontSize}">
                                        <TextBlock x:Name="TextToken"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=Foreground}"
                                                   FontSize="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=FontSize}"
                                                   FontWeight="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=FontWeight}"
                                                   Text="{Binding Text}" />

                                        <Viewbox x:Name="WindowsToken"
                                                 Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=FontSize}"
                                                 Height="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=FontSize}"
                                                 Visibility="Collapsed">
                                            <Path Stretch="Uniform"
                                                  Fill="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:Shortcut}}, Path=Foreground}"
                                                  Data="M7 7H0V0H7V7ZM15 7H8V0H15V7ZM7 15H0V8H7V15ZM15 15H8V8H15V15Z" />
                                        </Viewbox>
                                    </Grid>
                                </Border>

                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsWindowsKey}" Value="True">
                                        <Setter TargetName="TextToken" Property="Visibility" Value="Collapsed" />
                                        <Setter TargetName="WindowsToken" Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>