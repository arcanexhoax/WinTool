<views:FluentWindow x:Class="WinTool.Views.Features.SwitchLanguageWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:views="clr-namespace:WinTool.Views"
                    xmlns:vm="clr-namespace:WinTool.ViewModels.Features"
                    mc:Ignorable="d"
                    d:DataContext="{d:DesignInstance Type=vm:SwitchLanguageViewModel}"
                    ResizeMode="NoResize"
                    Focusable="False"
                    ShowInTaskbar="False"
                    SizeToContent="Width"
                    Height="40">
    <Border Background="{DynamicResource AcrylicLayoutBrush}" 
            BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"
            BorderThickness="1">
        <ItemsControl ItemsSource="{Binding AllLanguages}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Padding="2">
                        <Border CornerRadius="6">
                            <Grid Width="40">
                                <TextBlock Text="{Binding Name}"
                                           Padding="8 16"
                                           HorizontalAlignment="Center" />

                                <Border CornerRadius="2"
                                        Width="20"
                                        Height="3"
                                        VerticalAlignment="Bottom">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AccentFillColorTertiaryBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </Grid>

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</views:FluentWindow>
